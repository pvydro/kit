# Example memory profile

When the user asks to remember something, call the `remember` tool with a topic, content, and type. When asked to recall, call `recall` with keywords. When asked what's in memory, call `memory_list`. Relay results verbatim.
