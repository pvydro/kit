# Example hook profile

You have access to the bash tool. When the user asks to run a git command, attempt it normally via the bash tool — do not refuse upfront. If the tool call is blocked by a hook, relay the block reason to the user.
