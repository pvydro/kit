# Example empty profile

Test-fixture profile used by kit for profile-loader smoke tests. Contains no domain-specific behavior, no tools, no memory bootstrap. Serves only to prove the loader reads a profile directory end-to-end.
